# Tarefas: Retomada e Correção do Motor de Broadcasts

- [x] Corrigir `app/api/whatsapp/broadcast/send/route.js`
  - [x] Identify and resolve the trigger or application logic causing the `record "new" has no field "organizacao_id"` error.
- [x] Validate the structural integrity of the `sys_chat_messages` table.
- [x] Confirm WhatsApp Inbox filter updates remain fully functional.
- [x] Restore cross-module consistency between CRM, WhatsApp API, and internal chat interfaces. Visual)
  - [x] Alterar `getStatusInfo` ou lógica do botão para exibir "Retomar" mesmo em status `completed` se a lista tiver mais membros que os envios realizados.
  - [x] Adicionar suporte a chamar `handleControl(broadcast.id, 'resume', ...)` em um card já `completed` que possui faltantes.
- [x] Opcional (Se necessário) revisar se a CRON precisa de ajustes. Já vimos que ela pega `pending`.
