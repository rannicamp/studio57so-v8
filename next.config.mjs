// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  // O conteúdo da configuração "env" foi removido.
  // O Next.js já lê o arquivo .env.local automaticamente para o servidor.
};

export default nextConfig;